apiVersion: v1
kind: Service
metadata:
  name: enterprise-devsecops-service
  namespace: enterprise-devsecops
spec:
  selector:
    app: enterprise-devsecops-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
